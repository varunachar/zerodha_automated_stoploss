# Agent Instructions

- Before running any Python commands, always check if the Python environment is activated.
- If the environment is not activated, run `source ~/activate.sh` before running any project-specific Python commands.
